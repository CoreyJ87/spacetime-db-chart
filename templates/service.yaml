apiVersion: v1
kind: Service
metadata:
  name: {{ include "spacetimedb.fullname" . }}
  labels:
    {{- include "spacetimedb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spacetimedb.selectorLabels" . | nindent 4 }}
